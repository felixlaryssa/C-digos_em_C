#include <stdio.h>

int main() {
    int rows, cols;

    

   
printf("Digite o número de linhas: ");
    scanf("%d", &rows);
    printf("Digite o número de colunas: ");
    scanf("%d", &cols);

    int matriz[rows][cols];

    

   
// Preencha a matriz
    printf("Digite os elementos da matriz:\n");
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            scanf("%d", &matriz[i][j]);
        }
    }

    
        }
    }

   

        }
    }


        }
// Calcule e imprima a soma das linhas
    
   
for (int i = 0; i < rows; i++) {
        int soma = 0;
        for (int j = 0; j < cols; j++) {
            soma += matriz[i][j];
        }
        
            soma += matriz[i]
printf("Soma da linha %d: %d\n", i + 1, soma);
    }

    
    }

return 0;
}

}
``
//Este código permite que você insira o//





