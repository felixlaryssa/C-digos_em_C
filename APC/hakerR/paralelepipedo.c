#include <stdio.h>
#include <stdlib.h>
#define MAX_HEIGHT 41

struct box
{
	float comprimento;
    float largura;
    float altura;
    
};

typedef struct box box;

int get_volume(box b) {
	 
    int box b[n];

    for (int i=0; i)
    box b[i] = comprimento * largura * altura;
}

int is_lower_than_max_height(box b) {
	/**
	* Return 1 if the box's height is lower than MAX_HEIGHT and 0 otherwise
	*/
}

int main()

int n;
scanf("%d\n", &n);

int box b[3]